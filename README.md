# Quant-UX WebSpockeyt Server

The web socket server to allow collaborative editing in Quant-UX.

## Config

Set the following config params as ENV
```
export QUX_SERVER=https://backend.qux.your-server.com/

```
